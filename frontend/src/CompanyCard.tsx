import {useContext} from "react";
import {CompanyProvider} from "./CompanyContext";

export default function CompanyCard () {

    const context = useContext(CompanyProvider)

    return (
        <div>

        </div>

    )
}